<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.main.homePage.mapper.CarMapper">
    <!--
        <select id="membershipUserVO" parameterType="hashmap" resultType="MembershipUserVO"  >


            SELECT 1
              FROM

            <if test ="userCd != null and userCd != ''">
                AND USER_CD= #{userCd}
            </if>
            <if test ="mberNo != null and mberNo != ''">
                AND MBER_NO= #{mberNo}
            </if>
        </select>
    -->

</mapper>